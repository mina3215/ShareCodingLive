<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codragon.sclive.mapper.ConferenceMapper">

    <insert id="create" parameterType="ConCreateDao">
        INSERT INTO conference (`conference_uuid`, `start_time`, `title`, `is_active`, `owner_email`) VALUES (#{conferenceUuid}, #{startTime}, #{title}, 0, #{ownerEmail});
    </insert>

    <update id="start" parameterType="string">
        update conference set is_active=1 where conference_uuid = #{hello};
    </update>
</mapper>